# v3.3.4 Index Reliability Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 v3.3.3 的公司索引路径隔离、并发写入、公司统计、结论分类、无 JavaScript 降级和 HTML 转义缺陷，并形成可发布的 v3.3.4。

**Architecture:** 保持 Gate、HTML 渲染器和索引脚本现有边界。Gate 从实际 `run_root` 推导索引归属；索引脚本提供带跨平台锁和原子替换的唯一重建入口；页面使用渐进增强，核心内容默认可见。

**Tech Stack:** Python 3 标准库、`unittest`、HTML/CSS/JavaScript、现有 Gate 与统一检查脚本。

## Global Constraints

- 不新增第三方依赖。
- 不移动、删除或改写既有 `local/` 报告。
- 不改变 Contract v2、13 项业务契约、Audit、Review 或 APPROVED 语义。
- 不把 HTML 或索引纳入准出条件。
- 新运行统一使用 `local/Company/`，旧运行按实际父目录兼容。
- 每项生产代码必须先有失败测试。
- 只修改本计划列出的文件。
- 任何 push、PR、远端 tag 和 GitHub Release 均需当前对话的明确授权。

---

### Task 1: 运行路径隔离与新目录规范

**Files:**
- Modify: `tests/test_full_analysis_gate_v2.py`
- Modify: `tools/full_analysis_gate.py`

**Interfaces:**
- Consumes: `_rebuild_company_index(root: Path) -> bool`
- Produces: `_company_base_from_run_root(root: Path) -> Path`
- Produces: `build_run_root()` 使用 `local/Company`

- [ ] **Step 1: 写目录大小写和隔离失败测试**

在 `GateV2Tests` 增加：

```python
def test_build_run_root_uses_canonical_company_directory(self):
    root = gate_module.build_run_root(
        Path("/tmp/repo"), "600001.SH", "测试公司")
    self.assertEqual(root.parts[-4:-2], ("local", "Company"))

def test_rebuild_company_index_writes_to_run_owned_base(self):
    company_base = self.root / "local/Company"
    run_root = company_base / "600001.SH-测试公司/20260727-120000-aaaaaa"
    run_root.mkdir(parents=True)
    summary = run_root / "测试公司-全量分析-总结报告.md"
    summary.write_text(
        "# 测试公司\n\n一句话总结：建议持有。\n\n数据截止日：2026-07-27。",
        encoding="utf-8")

    self.assertTrue(gate_module._rebuild_company_index(run_root))
    self.assertTrue((company_base / "index.html").is_file())
```

- [ ] **Step 2: 运行测试并确认 RED**

Run:

```bash
python3 -m unittest \
  tests.test_full_analysis_gate_v2.GateV2Tests.test_build_run_root_uses_canonical_company_directory \
  tests.test_full_analysis_gate_v2.GateV2Tests.test_rebuild_company_index_writes_to_run_owned_base -v
```

Expected: 第一项显示 `company`；第二项未在临时公司根目录生成索引。

- [ ] **Step 3: 实现实际路径推导**

在 Gate 中增加：

```python
def _company_base_from_run_root(root: Path) -> Path:
    root = Path(root)
    company_dir = root.parent
    company_base = company_dir.parent
    if not root.is_dir() or not company_dir.is_dir() or not company_base.is_dir():
        raise GateError(f"无法从 run_root 推导公司目录: {root}")
    return company_base
```

将 `build_run_root()` 的目录改为：

```python
return repo_root / "local" / "Company" / f"{code}-{company}" / f"{stamp}-{short}"
```

`_rebuild_company_index()` 使用：

```python
company_base = _company_base_from_run_root(root)
```

生成器路径仍使用 `TOOLS_DIR.parent / "scripts/build_company_index.py"`。

- [ ] **Step 4: 运行 Gate 定向测试**

Run:

```bash
python3 -m unittest tests.test_full_analysis_gate_v2 -v
```

Expected: PASS，测试输出只位于临时目录。

- [ ] **Step 5: 精确提交**

```bash
git add tests/test_full_analysis_gate_v2.py tools/full_analysis_gate.py
git commit -m "fix(full-analysis): 隔离公司索引输出目录"
```

---

### Task 2: 统一索引事务和原子写入

**Files:**
- Modify: `tests/test_build_company_index.py`
- Modify: `tests/test_full_analysis_gate_v2.py`
- Modify: `scripts/build_company_index.py`
- Modify: `tools/full_analysis_gate.py`

**Interfaces:**
- Produces: `index_lock(base: Path)` context manager
- Produces: `atomic_write_text(path: Path, content: str) -> None`
- Produces: `rebuild_index(base: Path, output: Path | None = None) -> dict`
- Consumes: Gate 调用 `rebuild_index(company_base)`

- [ ] **Step 1: 写原子替换和统一入口失败测试**

在索引测试中增加：

```python
def test_rebuild_index_writes_complete_index(self):
    out = self.base / "index.html"
    result = idx.rebuild_index(self.base, out)
    self.assertEqual(result["index"], str(out))
    self.assertEqual(result["companies"], 2)
    self.assertEqual(out.read_text(encoding="utf-8").count("<article"), 2)

def test_atomic_write_failure_preserves_previous_index(self):
    out = self.base / "index.html"
    out.write_text("previous", encoding="utf-8")
    with mock.patch.object(idx.os, "replace", side_effect=OSError("replace failed")):
        with self.assertRaises(OSError):
            idx.atomic_write_text(out, "new")
    self.assertEqual(out.read_text(encoding="utf-8"), "previous")
```

增加 `from unittest import mock`。

- [ ] **Step 2: 运行测试并确认 RED**

Run:

```bash
python3 -m unittest \
  tests.test_build_company_index.BuildCompanyIndexTest.test_rebuild_index_writes_complete_index \
  tests.test_build_company_index.BuildCompanyIndexTest.test_atomic_write_failure_preserves_previous_index -v
```

Expected: `rebuild_index` 和 `atomic_write_text` 尚不存在。

- [ ] **Step 3: 实现标准库锁和原子文本写入**

索引脚本增加：

```python
import os
import tempfile
from contextlib import contextmanager

try:
    import fcntl
except ImportError:  # pragma: no cover - Windows compatibility
    fcntl = None
    import msvcrt


@contextmanager
def index_lock(base: Path):
    path = Path(base) / ".index.lock"
    with path.open("a+b") as handle:
        if fcntl is not None:
            fcntl.flock(handle.fileno(), fcntl.LOCK_EX)
        else:  # pragma: no cover - Windows compatibility
            handle.seek(0)
            if handle.read(1) == b"":
                handle.write(b"\0")
                handle.flush()
            handle.seek(0)
            msvcrt.locking(handle.fileno(), msvcrt.LK_LOCK, 1)
        try:
            yield
        finally:
            if fcntl is not None:
                fcntl.flock(handle.fileno(), fcntl.LOCK_UN)
            else:  # pragma: no cover - Windows compatibility
                handle.seek(0)
                msvcrt.locking(handle.fileno(), msvcrt.LK_UNLCK, 1)


def atomic_write_text(path: Path, content: str) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    fd, tmp_name = tempfile.mkstemp(prefix=f".{path.name}.", dir=path.parent)
    try:
        with os.fdopen(fd, "w", encoding="utf-8") as handle:
            handle.write(content)
            handle.flush()
            os.fsync(handle.fileno())
        os.replace(tmp_name, path)
    finally:
        if os.path.exists(tmp_name):
            os.unlink(tmp_name)
```

- [ ] **Step 4: 实现唯一重建入口**

```python
def rebuild_index(base: Path, output: Path | None = None) -> dict:
    base = Path(base)
    out = Path(output) if output else base / "index.html"
    with index_lock(base):
        warnings: list[str] = []
        rows = collect(base, warnings=warnings)
        if not rows:
            raise ValueError(f"{base} 下未找到任何总结报告")
        html = build_index_html(rows, base_label=str(base))
        atomic_write_text(out, html)
    return {
        "index": str(out),
        "companies": len(rows),
        "bytes": len(html.encode("utf-8")),
        "warnings": warnings,
    }
```

CLI 非 `--check` 分支改调 `rebuild_index()`；Gate 改调 `module.rebuild_index(company_base)`。

Gate 的单报告 HTML 写入使用现有临时文件模式新增 `atomic_write_text()`，替换 `html_path.write_text()`。

- [ ] **Step 5: 增加并发回归测试**

用 `ThreadPoolExecutor(max_workers=4)` 同时调用四次 `rebuild_index()`：

```python
def test_concurrent_rebuilds_leave_one_complete_index(self):
    from concurrent.futures import ThreadPoolExecutor

    with ThreadPoolExecutor(max_workers=4) as pool:
        results = list(pool.map(
            lambda _: idx.rebuild_index(self.base), range(8)))

    html = (self.base / "index.html").read_text(encoding="utf-8")
    self.assertEqual(html.count("<article"), html.count("</article>"))
    self.assertEqual(html.count("<article"), 2)
    self.assertTrue(all(item["companies"] == 2 for item in results))
```

- [ ] **Step 6: 运行索引和 Gate 测试**

Run:

```bash
python3 -m unittest tests.test_build_company_index tests.test_full_analysis_gate_v2 -v
```

Expected: PASS。

- [ ] **Step 7: 精确提交**

```bash
git add \
  tests/test_build_company_index.py \
  tests/test_full_analysis_gate_v2.py \
  scripts/build_company_index.py \
  tools/full_analysis_gate.py
git commit -m "fix(full-analysis): 原子化公司索引重建事务"
```

---

### Task 3: 最新公司运行、板块和结论分类

**Files:**
- Modify: `tests/test_build_company_index.py`
- Modify: `scripts/build_company_index.py`

**Interfaces:**
- Consumes: `collect(base: Path, *, warnings: list[str] | None = None) -> list[dict]`
- Produces: 每个公司目录最多一行最新运行
- Produces: `board_of()` 支持北交所
- Produces: `verdict_of()` 只使用明确短语

- [ ] **Step 1: 写分类和去重失败测试**

```python
def test_board_classification_includes_beijing_exchange(self):
    self.assertEqual(idx.board_of("920001.BJ"), "北交所")
    self.assertEqual(idx.board_of("430001.BJ"), "北交所")
    self.assertEqual(idx.board_of("830001.BJ"), "北交所")

def test_verdict_ignores_disclaimer_and_ambiguous_pass(self):
    self.assertEqual(idx.verdict_of("四大师一致 PASS，建议持有"), "关注")
    self.assertEqual(idx.verdict_of("本报告不构成投资建议"), "中性")
    self.assertEqual(idx.verdict_of("公司等待治理改善后再评估"), "等待")
    self.assertEqual(idx.verdict_of("业绩同比增长，建议关注"), "关注")

def test_collect_keeps_only_latest_run_per_company(self):
    _write_company(
        self.base, "600001.SH-测试甲", "20260727-120000-zzzzzz",
        "测试甲-全量分析-总结报告.md",
        "# 测试甲\n\n一句话总结：最新报告建议持有。\n")
    rows = [row for row in idx.collect(self.base)
            if row["company"] == "测试甲"]
    self.assertEqual(len(rows), 1)
    self.assertEqual(rows[0]["run"], "20260727-120000-zzzzzz")
    self.assertIn("最新报告", rows[0]["one"])
```

- [ ] **Step 2: 运行测试并确认 RED**

Run:

```bash
python3 -m unittest \
  tests.test_build_company_index.BuildCompanyIndexTest.test_board_classification_includes_beijing_exchange \
  tests.test_build_company_index.BuildCompanyIndexTest.test_verdict_ignores_disclaimer_and_ambiguous_pass \
  tests.test_build_company_index.BuildCompanyIndexTest.test_collect_keeps_only_latest_run_per_company -v
```

Expected: 北交所归为深主板，免责声明归为回避，同一公司返回两行。

- [ ] **Step 3: 修正规则**

```python
_NEG_KW = ("回避", "否决", "看空", "排除", "不建议买入", "不参与")
_POS_KW = ("买入", "持有", "建仓", "可买", "通过", "建议关注")
_WAIT_KW = (
    "等待", "等回调", "等估值", "等价格", "观望",
    "跟踪", "观察", "不买", "卫星仓位", "警惕", "跌出折扣",
)
```

`board_of()` 首先处理：

```python
raw = code.upper()
p = raw.split(".")[0]
if raw.endswith(".BJ") or p.startswith(("4", "8", "920")):
    return "北交所"
```

排序增加 `"北交所": 4`。

- [ ] **Step 4: 每家公司选择最新有效运行**

把公司目录内循环改为：

```python
runs = sorted(
    (path for path in cd.iterdir() if path.is_dir()),
    reverse=True,
)
selected = next(
    ((run, sorted(run.glob("*总结报告.md"))[-1])
     for run in runs if list(run.glob("*总结报告.md"))),
    None,
)
if selected is None:
    continue
run, md = selected
```

然后只生成这一行。

- [ ] **Step 5: 运行索引测试**

Run:

```bash
python3 -m unittest tests.test_build_company_index -v
```

Expected: PASS；每家公司一张卡片。

- [ ] **Step 6: 精确提交**

```bash
git add tests/test_build_company_index.py scripts/build_company_index.py
git commit -m "fix(full-analysis): 校正公司索引统计与分类"
```

---

### Task 4: manifest 错误可见性与 HTML 转义

**Files:**
- Modify: `tests/test_build_company_index.py`
- Modify: `scripts/build_company_index.py`

**Interfaces:**
- Consumes: `collect(..., warnings=warnings)`
- Produces: 损坏 manifest 的 `MANIFEST_ERROR` 状态和精确 warning
- Produces: `build_index_html()` 转义 `base_label`

- [ ] **Step 1: 写 manifest 和 base_label 失败测试**

```python
def test_corrupt_manifest_is_loud(self):
    run = self.base / "600001.SH-测试甲/20260725-000000-aaaaaa"
    manifest = run / "evidence/00-analysis-manifest.json"
    manifest.parent.mkdir(parents=True, exist_ok=True)
    manifest.write_text("{broken", encoding="utf-8")
    warnings: list[str] = []

    rows = idx.collect(self.base, warnings=warnings)
    row = next(item for item in rows if item["company"] == "测试甲")

    self.assertEqual(row["status"], "MANIFEST_ERROR")
    self.assertEqual(len(warnings), 1)
    self.assertIn(str(manifest), warnings[0])

def test_base_label_is_html_escaped(self):
    html = idx.build_index_html(
        idx.collect(self.base),
        base_label='<img src=x onerror="alert(1)">')
    self.assertNotIn('<img src=x onerror="alert(1)">', html)
    self.assertIn("&lt;img", html)
```

- [ ] **Step 2: 运行测试并确认 RED**

Run:

```bash
python3 -m unittest \
  tests.test_build_company_index.BuildCompanyIndexTest.test_corrupt_manifest_is_loud \
  tests.test_build_company_index.BuildCompanyIndexTest.test_base_label_is_html_escaped -v
```

Expected: status 为“深度总结”、warnings 为空、base label 未转义。

- [ ] **Step 3: 实现显式 warning**

签名改为：

```python
def collect(
    base: Path, *, warnings: list[str] | None = None
) -> list[dict]:
```

manifest 异常分支：

```python
except (OSError, json.JSONDecodeError, TypeError, AttributeError) as exc:
    status = "MANIFEST_ERROR"
    if warnings is not None:
        warnings.append(f"{mf}: {exc}")
```

状态赋值使用：

```python
"status": status if status == "MANIFEST_ERROR" else status or "深度总结",
```

CLI 和 `rebuild_index()` 将 warnings 逐条输出到 stderr。

- [ ] **Step 4: 转义 base label**

`build_index_html()` 开头增加：

```python
safe_base_label = _esc(base_label)
```

模板使用 `{safe_base_label}`。

- [ ] **Step 5: 运行索引测试**

Run:

```bash
python3 -m unittest tests.test_build_company_index -v
```

Expected: PASS。

- [ ] **Step 6: 精确提交**

```bash
git add tests/test_build_company_index.py scripts/build_company_index.py
git commit -m "fix(full-analysis): 显式报告索引元数据异常"
```

---

### Task 5: 无 JavaScript 渐进增强

**Files:**
- Modify: `tests/test_full_analysis_html.py`
- Modify: `tests/test_build_company_index.py`
- Modify: `tools/full_analysis_html.py`
- Modify: `scripts/build_company_index.py`

**Interfaces:**
- Produces: 核心内容默认可见
- Produces: JavaScript 可用时通过根元素 `js` 类启用 reveal 动画

- [ ] **Step 1: 写默认可见失败测试**

```python
def test_report_content_is_visible_without_javascript(self):
    page = html_module.build_summary_page(
        SAMPLE_MD, company="测试", code="600001.SH",
        as_of="2026-07-27", skill_count=13, status="APPROVED")
    self.assertNotIn(".reveal{opacity:0", page)
    self.assertIn(".js .reveal{opacity:0", page)
    self.assertIn("document.documentElement.classList.add('js')", page)
```

索引测试增加：

```python
def test_cards_are_visible_without_javascript(self):
    page = idx.build_index_html(idx.collect(self.base), base_label="x")
    self.assertNotIn(".card{position:relative", page.split("opacity:0")[0])
    self.assertIn(".js .card{opacity:0", page)
    self.assertIn("document.documentElement.classList.add('js')", page)
```

实现时可以把索引断言调整为精确检查 CSS 声明，但必须证明非 `.js` 选择器没有隐藏卡片。

- [ ] **Step 2: 运行测试并确认 RED**

Run:

```bash
python3 -m unittest \
  tests.test_full_analysis_html \
  tests.test_build_company_index.BuildCompanyIndexTest.test_cards_are_visible_without_javascript -v
```

Expected: 默认 CSS 仍把内容设为 `opacity:0`。

- [ ] **Step 3: 实现渐进增强**

报告 CSS：

```css
.reveal{transition:opacity .65s ease,transform .65s ease}
.js .reveal{opacity:0;transform:translateY(16px)}
.js .reveal.in{opacity:1;transform:none}
```

索引 CSS：

```css
.card{opacity:1;transform:none}
.js .card{opacity:0;transform:translateY(16px)}
.js .card.in{opacity:1;transform:none}
```

两个页面的 `<head>` 在样式前加入：

```html
<script>document.documentElement.classList.add('js')</script>
```

`prefers-reduced-motion` 和打印 CSS 同时覆盖 `.js .reveal`、`.js .card`。

- [ ] **Step 4: 运行 HTML 和索引测试**

Run:

```bash
python3 -m unittest tests.test_full_analysis_html tests.test_build_company_index -v
```

Expected: PASS。

- [ ] **Step 5: 精确提交**

```bash
git add \
  tests/test_full_analysis_html.py \
  tests/test_build_company_index.py \
  tools/full_analysis_html.py \
  scripts/build_company_index.py
git commit -m "fix(full-analysis): 保证展示件无脚本可读"
```

---

### Task 6: 集成验证和 v3.3.4 发布准备

**Files:**
- Modify: `tests/test_full_analysis_e2e.py`
- Modify: `CHANGELOG.md`
- Create: `.github/release-notes/v3.3.4.md`

**Interfaces:**
- Produces: 临时仓库端到端索引隔离证据
- Produces: v3.3.4 发布说明

- [ ] **Step 1: 增加端到端索引归属断言**

在现有临时仓库批准流程生成 HTML 后增加：

```python
company_base = run_root.parent.parent
index_path = company_base / "index.html"
self.assertTrue(index_path.is_file())
index_html = index_path.read_text(encoding="utf-8")
self.assertIn(company_name, index_html)
self.assertIn(code, index_html)
```

测试不得读取或写入仓库真实 `local/Company`。

- [ ] **Step 2: 运行 Gate、HTML、索引和 e2e 测试**

Run:

```bash
python3 -m unittest \
  tests.test_full_analysis_gate_v2 \
  tests.test_full_analysis_html \
  tests.test_build_company_index \
  tests.test_full_analysis_e2e -v
```

Expected: PASS，所有输出位于临时目录。

- [ ] **Step 3: 更新 changelog 和 release notes**

发布说明必须包含：

- `run_root` 路径隔离和 `Company` 规范。
- 索引锁与原子替换。
- 最新运行去重、北交所和结论分类。
- manifest 错误可见性。
- 无 JavaScript 可读和 `base_label` 转义。
- 不改变 Contract、APPROVED 和既有报告内容。
- 最终实际测试数量。

- [ ] **Step 4: 运行生成物和差异检查**

Run:

```bash
python3 scripts/sync-codex-skills.py --check
git diff --check
```

Expected: 生成物同步且无空白错误。

- [ ] **Step 5: 运行统一检查**

Run:

```bash
bash scripts/check.sh
```

Expected: 全部单元测试、frontmatter、Codex skill 同步、Contract 注册表和本地索引检查通过。

- [ ] **Step 6: 检查工作区范围**

Run:

```bash
git status --short
git diff --stat v3.3.3...HEAD
git diff --check v3.3.3...HEAD
git log --oneline v3.3.3..HEAD
```

Expected: 只包含计划文件、实现、测试、CHANGELOG 和 v3.3.4 release notes；没有 `local/` 报告或无关改动。

- [ ] **Step 7: 精确提交发布文档**

```bash
git add tests/test_full_analysis_e2e.py CHANGELOG.md .github/release-notes/v3.3.4.md
git commit -m "docs: 补充 v3.3.4 发布说明"
```

- [ ] **Step 8: 最终独立验证**

Run:

```bash
bash scripts/check.sh
git status --short --branch
```

Expected: 完整检查通过，工作树干净。

---

## Plan Self-Review

- Spec coverage: 设计规格的 9 项成功标准分别由 Task 1–6 覆盖。
- Scope: 不包含数据迁移、Contract 变更、准出规则修改、第三方依赖或无关重构。
- Interface consistency: `collect(..., warnings=...)`、`rebuild_index()`、`atomic_write_text()` 和 Gate 调用在任务间一致。
- Isolation: 所有 Gate 和索引集成测试使用临时目录，不依赖真实 `local/Company`。
- Verification: 每项行为变化都有明确 RED/GREEN 命令，最终统一检查和差异审计重复验证。
- Release safety: 本计划只完成本地分支和发布材料，不自动执行任何外部写入。
