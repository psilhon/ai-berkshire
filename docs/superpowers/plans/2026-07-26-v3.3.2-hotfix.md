# v3.3.2 Hotfix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 v3.3.1 的 Result Bundle 校验与 ingest 一致性、FAILED 终态、派发模板和 HTML 安全缺陷，并形成可发布的 v3.3.2。

**Architecture:** 保持 Contract v2、Result Bundle v1 和现有模块边界。Gate 在写正式文件前完成递归 schema 校验与内存态 provenance 准备；Runtime 继续派发单一文本 payload；HTML 保持标准库渲染但增加协议白名单与统一 escaping。

**Tech Stack:** Python 3 标准库、`unittest`、JSON、Markdown、现有同步与审计脚本。

## Global Constraints

- 不新增第三方依赖。
- 不改变 Contract v2、Result Bundle v1、13 项业务契约或 WorkBuddy 生产入口。
- 不恢复已删除 skill，不改写旧 tag。
- 每项生产代码必须先有失败测试。
- 只修改本计划列出的文件；生成文件必须由 `scripts/sync-codex-skills.py` 更新。
- 任何 push、PR、release tag 和 GitHub Release 均按发布安全规则执行。

---

### Task 1: Result Bundle 深度校验与 ingest 提交顺序

**Files:**
- Modify: `tests/test_full_analysis_gate_v2.py`
- Modify: `tools/full_analysis_gate.py`

**Interfaces:**
- Consumes: `tools/full_analysis_result_schema.json`
- Produces: `_validate_schema_value(value, schema, path="$") -> None`
- Produces: `validate_result_bundle()` 在任何 artifact 写入前拒绝嵌套非法数据

- [ ] **Step 1: 写 malformed bundle 失败回归测试**

在 `GateV2Tests` 增加：

```python
def test_malformed_nested_bundle_is_rejected_before_formal_copy(self):
    self.init()
    skill_id = "ashare-data"
    bp, rel, size, digest = self._write_attempt(
        skill_id, "attempt-bad", build_compliant_report(REGISTRY, skill_id))
    bundle = self._bundle(
        skill_id=skill_id, attempt_id="attempt-bad",
        artifact_id="artifact.ashare-data", rel=rel, size=size, digest=digest)
    bundle["fact_updates"] = ["not-an-object"]

    result = self._ingest(bp, bundle)

    self.assertNotEqual(result.returncode, 0)
    self.assertIn("$.fact_updates[0]", result.stdout + result.stderr)
    self.assertFalse(
        (self.run_root / "01-数据与快筛/01-ashare-data.md").exists())
    manifest = json.loads(
        (self.run_root / "evidence/00-analysis-manifest.json").read_text())
    entry = next(s for s in manifest["skills"] if s["skill_id"] == skill_id)
    self.assertEqual(entry["status"], "PENDING")
```

- [ ] **Step 2: 运行测试并确认 RED**

Run:

```bash
python3 -m unittest tests.test_full_analysis_gate_v2.GateV2Tests.test_malformed_nested_bundle_is_rejected_before_formal_copy -v
```

Expected: FAIL；正式文件已经存在或错误信息缺少 JSON 路径。

- [ ] **Step 3: 实现递归 schema 校验**

在 Gate 中增加标准库实现，覆盖当前 schema 使用的关键字：

```python
def _schema_type_matches(value, expected: str) -> bool:
    if expected == "object":
        return isinstance(value, dict)
    if expected == "array":
        return isinstance(value, list)
    if expected == "string":
        return isinstance(value, str)
    if expected == "integer":
        return isinstance(value, int) and not isinstance(value, bool)
    if expected == "boolean":
        return isinstance(value, bool)
    if expected == "null":
        return value is None
    return True


def _validate_schema_value(value, schema: dict, path: str = "$") -> None:
    expected = schema.get("type")
    expected_types = expected if isinstance(expected, list) else [expected]
    if expected and not any(_schema_type_matches(value, item)
                            for item in expected_types):
        raise GateError(f"{path} 类型非法，期望 {expected}")
    if "const" in schema and value != schema["const"]:
        raise GateError(f"{path} 必须等于 {schema['const']!r}")
    if "enum" in schema and value not in schema["enum"]:
        raise GateError(f"{path} 不在允许枚举中")
    if isinstance(value, str):
        if len(value) < schema.get("minLength", 0):
            raise GateError(f"{path} 长度不足")
        if schema.get("pattern") and not re.search(schema["pattern"], value):
            raise GateError(f"{path} 格式非法")
    if isinstance(value, int) and not isinstance(value, bool):
        if "minimum" in schema and value < schema["minimum"]:
            raise GateError(f"{path} 小于最小值 {schema['minimum']}")
    if isinstance(value, list):
        if len(value) < schema.get("minItems", 0):
            raise GateError(f"{path} 项目数不足")
        if schema.get("uniqueItems"):
            encoded = [json.dumps(item, ensure_ascii=False, sort_keys=True)
                       for item in value]
            if len(encoded) != len(set(encoded)):
                raise GateError(f"{path} 含重复项目")
        item_schema = schema.get("items")
        if item_schema:
            for index, item in enumerate(value):
                _validate_schema_value(item, item_schema, f"{path}[{index}]")
    if isinstance(value, dict):
        required = set(schema.get("required", []))
        missing = sorted(required - set(value))
        if missing:
            raise GateError(f"{path} 缺字段 {missing}")
        properties = schema.get("properties", {})
        if schema.get("additionalProperties") is False:
            extra = sorted(set(value) - set(properties))
            if extra:
                raise GateError(f"{path} 含未知字段 {extra}")
        for key, item in value.items():
            if key in properties:
                _validate_schema_value(item, properties[key], f"{path}.{key}")
```

`validate_result_bundle()` 读取 schema 后首先调用：

```python
_validate_schema_value(bundle, schema)
```

- [ ] **Step 4: 在内存副本上准备 provenance**

在正式复制前：

```python
next_manifest = copy.deepcopy(manifest)
entry = next(
    item for item in next_manifest["skills"]
    if item["skill_id"] == bundle["skill_id"])
entry.update({
    "status": bundle["status"],
    "attempts": [*entry.get("attempts", []), bundle["attempt_id"]],
    "artifact_records": prepared_records,
    "limitations": bundle["limitations"],
    "not_applicable": bundle.get("not_applicable"),
    "updated_at": now_iso(),
})
_merge_provenance(
    next_manifest, bundle, verified_role_runs=verified_role_runs)
```

所有内存准备成功后才执行 `atomic_copy()`，然后保存 `next_manifest`。

- [ ] **Step 5: 运行 Gate 与 schema 测试**

Run:

```bash
python3 -m unittest tests.test_full_analysis_gate_v2 tests.test_full_analysis_result_schema -v
```

Expected: PASS。

- [ ] **Step 6: 精确提交**

```bash
git add tests/test_full_analysis_gate_v2.py tools/full_analysis_gate.py
git commit -m "fix(full-analysis): 封闭结果校验与晋级事务边界"
```

---

### Task 2: FAILED 终态

**Files:**
- Modify: `tests/test_full_analysis_gate_v2.py`
- Modify: `tools/full_analysis_gate.py`

**Interfaces:**
- Produces: `SUCCESS_TERMINAL_STATUSES`
- Produces: `COMPLETED_STATUSES`
- Produces: finalize 对完成但失败的 run 写入 `FAILED` 并返回非零

- [ ] **Step 1: 写失败终态测试**

```python
def test_finalize_closes_completed_failed_run_as_failed(self):
    self.init()
    manifest_path = self.run_root / "evidence/00-analysis-manifest.json"
    manifest = json.loads(manifest_path.read_text())
    for item in manifest["skills"]:
        item["status"] = "NOT_APPLICABLE"
    manifest["skills"][0]["status"] = "FAIL"
    manifest_path.write_text(json.dumps(manifest), encoding="utf-8")

    result = run_gate(
        self.root, "finalize", "--run-root", self.run_root,
        "--registry", REGISTRY)

    self.assertNotEqual(result.returncode, 0)
    final = json.loads(manifest_path.read_text())
    self.assertEqual(final["run"]["status"], "FAILED")
    self.assertIn("ashare-data", result.stdout + result.stderr)
```

- [ ] **Step 2: 运行测试并确认 RED**

Run:

```bash
python3 -m unittest tests.test_full_analysis_gate_v2.GateV2Tests.test_finalize_closes_completed_failed_run_as_failed -v
```

Expected: FAIL；实际状态为 `PARTIAL`。

- [ ] **Step 3: 实现状态集合和 finalize 分支**

```python
SUCCESS_TERMINAL_STATUSES = {
    "PASS", "PASS_WITH_LIMITATIONS", "NOT_APPLICABLE",
}
COMPLETED_STATUSES = SUCCESS_TERMINAL_STATUSES | {"FAIL"}
```

业务校验继续使用成功集合；finalize 先识别真正未完成状态，再处理失败：

```python
pending = [
    item["skill_id"] for item in manifest["skills"]
    if item["status"] not in COMPLETED_STATUSES
]
failed = [
    item["skill_id"] for item in manifest["skills"]
    if item["status"] == "FAIL"
]
if pending:
    ...
if failed:
    manifest["run"]["status"] = "FAILED"
    save_manifest(root, manifest)
    append_event(root, {
        "type": "run_finalized", "status": "FAILED",
        "failed_skills": failed,
    })
    print(json.dumps({
        "run_root": str(root), "status": "FAILED",
        "failed_skills": failed,
    }, ensure_ascii=False))
    return 1
```

- [ ] **Step 4: 运行 Gate 测试**

Run:

```bash
python3 -m unittest tests.test_full_analysis_gate_v2 -v
```

Expected: PASS。

- [ ] **Step 5: 精确提交**

```bash
git add tests/test_full_analysis_gate_v2.py tools/full_analysis_gate.py
git commit -m "fix(full-analysis): 允许失败运行确定性终结"
```

---

### Task 3: 派发模板与方法论一致性

**Files:**
- Modify: `tests/test_full_analysis_runtime.py`
- Modify: `tests/test_full_analysis_docs.py`
- Modify: `tools/full_analysis_runtime.py`
- Modify: `skills/investment-checklist.md`
- Regenerate: `codex-skills/investment-checklist/SKILL.md`

**Interfaces:**
- Produces: 完整且不自证 Gate 接受状态的 `RESULT_BUNDLE_TEMPLATE`
- Produces: evidence 文案只要求满足当前 skill 的 `evidence_rules`

- [ ] **Step 1: 写模板和方法论失败测试**

```python
def test_next_work_injects_complete_result_bundle_template(self):
    self.start()
    payload = json.loads(
        self.cli("next-work", "--run-root", self.run_root).stdout)
    methodology = payload["methodology_text"]
    self.assertIn('"capability_records": []', methodology)
    self.assertIn('"not_applicable": null', methodology)
    self.assertIn('"accepted": false', methodology)
    self.assertNotIn('"formal": false,\\n      "accepted": true', methodology)
    self.assertIn("是否非空以 evidence_rules 为准", methodology)
```

在文档测试中断言投资检查方法论不再硬编码 3000：

```python
def test_investment_checklist_does_not_duplicate_contract_byte_floor(self):
    text = (REPO / "skills/investment-checklist.md").read_text(
        encoding="utf-8")
    self.assertNotIn("不少于 3000 字节", text)
    self.assertIn("Contract", text)
```

- [ ] **Step 2: 运行测试并确认 RED**

Run:

```bash
python3 -m unittest \
  tests.test_full_analysis_runtime.RuntimeTests.test_next_work_injects_complete_result_bundle_template \
  tests.test_full_analysis_docs.FullAnalysisDocumentationTests.test_investment_checklist_does_not_duplicate_contract_byte_floor -v
```

Expected: 两项 FAIL。

- [ ] **Step 3: 修正模板和证据指令**

模板 artifact 改为：

```json
"formal": false,
"accepted": false
```

并加入：

```json
"capability_records": [],
"not_applicable": null
```

说明 N/A 时的完整结构：

```json
{
  "predicate": "<contract applicability.predicate>",
  "fact_id": "<证明谓词为假的 fact_id>",
  "alternative": "<contract applicability.alternative 或 null>"
}
```

证据指令改为“字段必须存在；数组是否非空及最低数量以当前 skill 的 `evidence_rules` 为准”。

- [ ] **Step 4: 修正方法论并同步**

将固定 3000 字节文案改为：

```markdown
- 🔴 **通过防坍塌地板**：报告须完整覆盖六关分析与总判；具体字节地板以 `tools/full_analysis_contract.json` 的当前 Contract 为唯一机器真源，不在方法论中复制数值。
```

Run:

```bash
python3 scripts/sync-codex-skills.py
python3 scripts/sync-codex-skills.py --check
```

Expected: 生成包同步检查通过。

- [ ] **Step 5: 运行 Runtime 与文档测试**

Run:

```bash
python3 -m unittest tests.test_full_analysis_runtime tests.test_full_analysis_docs -v
```

Expected: PASS。

- [ ] **Step 6: 精确提交**

```bash
git add \
  tests/test_full_analysis_runtime.py \
  tests/test_full_analysis_docs.py \
  tools/full_analysis_runtime.py \
  skills/investment-checklist.md \
  codex-skills/investment-checklist/SKILL.md
git commit -m "fix(full-analysis): 补全派发模板与证据纪律"
```

---

### Task 4: HTML 安全、元数据、列表和章节诊断

**Files:**
- Modify: `tests/test_full_analysis_gate_v2.py`
- Modify: `tools/full_analysis_gate.py`

**Interfaces:**
- Produces: `_safe_link(url: str) -> str | None`
- Produces: `_render_html_page()` 对元数据完整 escape
- Produces: `_markdown_to_html()` 正确闭合列表并拒绝危险链接

- [ ] **Step 1: 写 HTML 和诊断失败测试**

```python
def test_markdown_html_rejects_dangerous_links_and_closes_ordered_list(self):
    html = gate_module._markdown_to_html(
        '1. 第一\\n2. 第二\\n\\n'
        '[危险](https://safe.example/\\" onmouseover=\\"alert(1))\\n'
        '[脚本](javascript:alert%281%29)')
    self.assertIn("<ol>", html)
    self.assertIn("</ol>", html)
    self.assertNotIn("onmouseover", html)
    self.assertNotIn("javascript:", html)

def test_html_page_escapes_metadata(self):
    html = gate_module._render_html_page(
        title='<img src=x onerror="alert(1)">',
        date_str="2026-07-26",
        status='APPROVED"><script>alert(1)</script>',
        tokens_css="", body="", skill_count=13)
    self.assertNotIn("<img", html)
    self.assertNotIn("<script>", html)
    self.assertIn("&lt;img", html)

def test_substance_diagnostic_skips_blank_lines_before_h3(self):
    skill = {
        "sections": [{
            "section_id": "core", "heading": "核心结论",
            "required": True, "min_content_chars": 150,
        }],
        "min_substantive_sections": 1,
    }
    errors = gate_module._substance_errors(
        skill, "## 核心结论\\n\\n### 子标题\\n正文")
    self.assertTrue(any("后紧跟 ###" in item for item in errors))
```

- [ ] **Step 2: 运行测试并确认 RED**

Run:

```bash
python3 -m unittest \
  tests.test_full_analysis_gate_v2.GateV2Tests.test_markdown_html_rejects_dangerous_links_and_closes_ordered_list \
  tests.test_full_analysis_gate_v2.GateV2Tests.test_html_page_escapes_metadata \
  tests.test_full_analysis_gate_v2.GateV2Tests.test_substance_diagnostic_skips_blank_lines_before_h3 -v
```

Expected: FAIL。

- [ ] **Step 3: 实现安全链接与 escaping**

使用：

```python
from html import escape as html_escape
from urllib.parse import urlsplit


def _safe_link(url: str) -> str | None:
    try:
        scheme = urlsplit(url).scheme.lower()
    except ValueError:
        return None
    if scheme not in {"http", "https", "mailto"}:
        return None
    return html_escape(url, quote=True)
```

`_inline_md()` 对非法 URL 只保留已转义的链接文本；`_render_html_page()` 在插值前转义 title、date 和 status。

- [ ] **Step 4: 修正元数据、列表和诊断**

HTML 元数据：

```python
company_info = manifest.get("company") or {}
company = company_info.get("name", "")
code = company_info.get("code", "")
date_str = (manifest.get("run") or {}).get("as_of", "")
```

列表使用 `list_tag: str | None`，打开和关闭同一种标签。章节诊断从 H2 后向前查找下一个非空行。

- [ ] **Step 5: 增加生成 HTML 元数据集成断言并运行 Gate/e2e**

在现有批准 canary 后断言 HTML 文件存在，且包含公司、代码和 `as_of`。

Run:

```bash
python3 -m unittest tests.test_full_analysis_gate_v2 tests.test_full_analysis_e2e -v
```

Expected: PASS。

- [ ] **Step 6: 精确提交**

```bash
git add tests/test_full_analysis_gate_v2.py tests/test_full_analysis_e2e.py tools/full_analysis_gate.py
git commit -m "fix(full-analysis): 加固 HTML 展示与章节诊断"
```

---

### Task 5: v3.3.2 文档、同步和发布验证

**Files:**
- Modify: `CHANGELOG.md`
- Create: `.github/release-notes/v3.3.2.md`
- Modify generated files only if sync requires them

**Interfaces:**
- Produces: v3.3.2 发布说明
- Produces: 可复现的统一验证证据

- [ ] **Step 1: 更新 changelog 与 release notes**

发布摘要必须包含：

- Result Bundle 深度校验和事务边界修复。
- FAILED 终态修复。
- 派发模板 capability/N/A 修复。
- HTML 安全、元数据、列表与章节诊断修复。
- 兼容性说明：保持 Contract v2/13 项/WorkBuddy，不恢复旧 run 兼容。
- 验证实际测试数量，以最终命令输出为准。

- [ ] **Step 2: 运行生成物和差异检查**

```bash
python3 scripts/sync-codex-skills.py --check
git diff --check
```

Expected: PASS，且无生成物漂移。

- [ ] **Step 3: 运行统一检查**

```bash
bash scripts/check.sh
```

Expected: 所有单元测试、frontmatter、Codex 同步、13 项契约和报告索引通过。

- [ ] **Step 4: 检查变更范围和 secret**

```bash
git status --short
git diff --stat v3.3.1...HEAD
git diff --check v3.3.1...HEAD
```

检查 staged 文件仅包含本计划范围，不读取 `.env` 或凭据文件。

- [ ] **Step 5: 提交发布文档**

```bash
git add CHANGELOG.md .github/release-notes/v3.3.2.md
git commit -m "docs: 补充 v3.3.2 发布说明"
```

- [ ] **Step 6: 最终验证**

```bash
bash scripts/check.sh
git status --short --branch
git log --oneline v3.3.1..HEAD
```

Expected: 检查通过，工作树干净，提交范围完整。

---

## Plan Self-Review

- Spec coverage: 设计文档的 8 项成功标准分别由 Task 1–5 覆盖。
- Scope: 未包含平台恢复、旧 skill 恢复、第三方依赖或模块重构。
- Type consistency: schema 校验、状态集合、HTML helper 和测试名称在各任务内一致。
- Verification: 每个行为变化均有明确 RED/GREEN 命令，最终统一检查重复执行。
